{
  "name": "megaore-v2",
  "version": "2.0.0",
  "description": "MegaORE Protocol V2 — drand VRF via Goldsky Compose",
  "scripts": {
    "compile": "hardhat compile",
    "deploy": "hardhat run scripts/deploy.js --network megaeth",
    "verify": "hardhat run scripts/verify.js --network megaeth",
    "resolver": "node scripts/resolver-bot.js",
    "status": "hardhat run scripts/check-status.js --network megaeth"
  },
  "dependencies": {
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "@openzeppelin/contracts": "^5.1.0",
    "dotenv": "^16.4.0",
    "ethers": "^6.13.0",
    "hardhat": "^2.22.0"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-ignition-ethers": "^3.0.7"
  }
}
